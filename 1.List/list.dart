void main(List<String> args) {
  var name = ['Nowrin', 'Israt', 'Ara', '25'];
  print(name);
  var firstItem = name.first;
  var lastItem = name.last;
  var reverse = name.reversed;
  var len = name.length;

  print(firstItem);
  print(lastItem);
  print(reverse);
  print(len);
  ;
}
